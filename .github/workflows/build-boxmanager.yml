name: build-boxmanager

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/build-boxmanager.yml"
      - "src/boxmanager/**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/boxmanager

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27c
          add-to-path: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/boxmanager

      - name: Build Android arm64 binary
        env:
          CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER: ${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android21-clang
        run: cargo build --release --target aarch64-linux-android

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: boxmanager-android-arm64
          path: src/boxmanager/target/aarch64-linux-android/release/boxmanager
